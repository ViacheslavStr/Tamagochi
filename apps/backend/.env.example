PORT=3001

# JWT Secret (change in production!)
JWT_SECRET=your-secret-key-change-in-production

# Database (PostgreSQL)
# Separate PostgreSQL 17 cluster on port 5433
# Cluster created at: ~/postgres-tamagochi
# Start: pg_ctl -D ~/postgres-tamagochi -l ~/postgres-tamagochi/logfile start
# Or: ./scripts/start-postgres-tamagochi.sh
# Replace YOUR_USERNAME with your macOS username
DATABASE_URL=postgresql://YOUR_USERNAME@localhost:5433/tamagochi

# Replicate API (for face generation)
REPLICATE_API_TOKEN=

# OpenAI API (for chat and TTS)
OPENAI_API_KEY=

# D-ID API (optional, for talking head)
DID_API_KEY=

# ElevenLabs API (optional, alternative to OpenAI TTS)
ELEVENLABS_API_KEY=

# Production: Frontend URL for CORS (in dev mode all origins are allowed)
# FRONTEND_URL=https://your-frontend-domain.com
